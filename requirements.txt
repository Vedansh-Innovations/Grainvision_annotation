# CPU / web dependencies (what Azure installs when deploying from GitHub).
# No torch/SAM2 here — segmentation runs on the light OpenCV engine, or is
# offloaded to a GPU worker via SAM2_REMOTE_URL. For SAM2 inside this process
# (Mode 3), also install requirements-gpu.txt.
Django==5.0.6
djangorestframework==3.15.1
django-environ==0.11.2
bcrypt==4.1.3

numpy==1.26.4
opencv-python-headless==4.9.0.80
Pillow==10.3.0
scikit-image==0.23.2
scipy==1.13.1
shapely==2.0.4
requests==2.32.3

gunicorn==22.0.0
whitenoise==6.7.0
boto3==1.34.122
django-storages==1.14.3
psycopg2-binary==2.9.9

# --- SAM2 on CPU (Mode 3). Lets SAM2 pull a compatible torch/torchvision.
# Remove this line to go back to the light OpenCV engine (smaller/faster build). ---
git+https://github.com/facebookresearch/sam2.git@main
