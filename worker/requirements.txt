# GPU worker deps. torch + sam2 come from the CUDA base image (see Dockerfile);
# these are the service libraries on top.
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==10.3.0
git+https://github.com/facebookresearch/sam2.git@main
