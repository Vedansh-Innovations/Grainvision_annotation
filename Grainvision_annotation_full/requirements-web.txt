# Lean web-tier deps for the SPLIT deployment (segmentation offloaded to the GPU
# worker via SAM2_REMOTE_URL). No torch / SAM2 here, so the image is small and
# starts fast on a cheap CPU plan. Use requirements.txt instead if you want the
# web app to run SAM2 itself.
Django==5.0.6
djangorestframework==3.15.1
django-environ==0.11.2
bcrypt==4.1.3

numpy==1.26.4
opencv-python-headless==4.9.0.80
Pillow==10.3.0
scikit-image==0.23.2
shapely==2.0.4
requests==2.32.3            # calls the GPU worker

gunicorn==22.0.0
whitenoise==6.7.0
boto3==1.34.122
django-storages==1.14.3
psycopg2-binary==2.9.9
