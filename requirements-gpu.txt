# Extra deps to run SAM2 INSIDE this process (Mode 3 / SAM2-on-CPU or local GPU).
# Let SAM2 pull a compatible torch/torchvision instead of pinning them here.
# For a CUDA build choose the matching torch from https://pytorch.org first.
git+https://github.com/facebookresearch/sam2.git@main
